export const UI_CONFIG = Object.freeze({
  maxContentWidthPx: 1280,
  minControlSizePx: 44,
  fastMotionMs: 120,
  standardMotionMs: 180,
  longMotionMs: 320,
  toastDurationMs: 4_000,
  timerWarningSeconds: 3,
} as const);
